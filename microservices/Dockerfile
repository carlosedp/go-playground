FROM scratch
ARG TARGETARCH
ARG TARGETOS
ARG BIN
ENV TARGETOS ${TARGETOS}
ENV TARGETARCH ${TARGETARCH}
ENV BIN ${BIN}

COPY ${BIN}-${TARGETOS}-${TARGETARCH} /${BIN}

EXPOSE 8080

ENTRYPOINT ["/${BIN}"]

